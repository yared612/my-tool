{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend && NPM_CONFIG_PRODUCTION=false npm install && npm run build && cp -r build/* ../backend/static/"
  },
  "dependencies": {},
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
